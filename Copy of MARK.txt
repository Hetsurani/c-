#include<stdio.h>
#include<conio.h>
#include<iostream.h>
void main()
{
    int m1, m2, m3;

    clrscr();

    printf("Enter marks of 3 subjects: ");
    scanf("%d%d%d", &m1, &m2, &m3);

    if(m1 >= 40 && m2 >= 40 && m3 >= 40)
        printf("PASS");

    else if((m1 < 40 && m2 >= 40 && m3 >= 40) ||
            (m1 >= 40 && m2 < 40 && m3 >= 40) ||
            (m1 >= 40 && m2 >= 40 && m3 < 40))
        printf("ATKT");

    else
        printf("FAIL");

    getch();
}